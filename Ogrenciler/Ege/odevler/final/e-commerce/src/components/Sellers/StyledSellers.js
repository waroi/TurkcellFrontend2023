import styled from "styled-components";

const StyledSellers = styled.div`
  .title {
    font-size: 16px;
    .highlight {
      font-size: 24px;
      font-weight: 600;
      color: #003459;
    }
  }
`;

export default StyledSellers;
